<Window x:Class="ElasticDesktopManager.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Style="{StaticResource WindowBaseStyle}">
    <StackPanel Margin="28,24">
        <StackPanel Orientation="Horizontal">
            <Border Width="52" Height="52" CornerRadius="10" Background="{DynamicResource AccentBrush}">
                <TextBlock Text="ES" FontSize="22" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="AppTitle" FontSize="18" FontWeight="Bold" />
                <TextBlock x:Name="VersionText" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
            </StackPanel>
        </StackPanel>

        <TextBlock x:Name="AuthorText" Margin="0,16,0,0" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}" />
        <TextBlock x:Name="DescText" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13" LineHeight="20" />
        <TextBlock x:Name="TechText" Margin="0,10,0,0" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button x:Name="GithubButton" Click="OnGithub" />
            <Button x:Name="CloseButton" Width="92" Margin="10,0,0,0" Click="OnClose" />
        </StackPanel>
    </StackPanel>
</Window>