<UserControl x:Class="ElasticDesktopManager.Views.NodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ElasticDesktopManager.Views"
             xmlns:icons="clr-namespace:ElasticDesktopManager.Services">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12" Visibility="{Binding HasConnection, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleText" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Grid.Column="1" x:Name="SummaryText" VerticalAlignment="Center" Margin="16,0,0,0"
                       FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="{Binding Summary, Mode=OneWay}" />
            <TextBox Grid.Column="2" Width="220" VerticalContentAlignment="Center"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
            <Button x:Name="RefreshButton" Grid.Column="3" Margin="8,0,0,0" Style="{StaticResource IconButtonStyle}"
                    Click="OnRefresh">
                        <Path Style="{StaticResource IconPathStyle}"
                              Data="{x:Static icons:AppIconGeometries.Refresh}"
                              Stroke="{DynamicResource TextSecondaryBrush}" />
                    </Button>
        </Grid>

        <!-- 表头文案由 code-behind 按当前语言赋值（见 NodesView.xaml.cs 的 NodeHeaders） -->
        <DataGrid x:Name="NodeGrid" Grid.Row="1" ItemsSource="{Binding Nodes}" IsReadOnly="True" SelectionMode="Single"
                  Visibility="{Binding HasConnection, Converter={StaticResource BoolToVisibility}}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Width="150" />
                <DataGridTextColumn Binding="{Binding Ip}" Width="130" />
                <DataGridTextColumn Binding="{Binding Port}" Width="70" />
                <DataGridTextColumn Binding="{Binding Version}" Width="90" />
                <DataGridTextColumn Binding="{Binding NodeRole}" Width="110" />
                <DataGridTextColumn Binding="{Binding Master}" Width="70" />
                <DataGridTextColumn Binding="{Binding Cpu}" Width="70" />
                <DataGridTextColumn Binding="{Binding HeapPercent}" Width="70" />
                <DataGridTextColumn Binding="{Binding RamPercent}" Width="70" />
                <DataGridTextColumn Binding="{Binding DiskUsedPercent}" Width="70" />
                <DataGridTextColumn Binding="{Binding Load1m}" Width="70" />
                <DataGridTextColumn Binding="{Binding Uptime}" Width="90" />
                <DataGridTextColumn Binding="{Binding Jdk}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <views:EmptyStateView Grid.Row="1" Grid.RowSpan="2"
                              Visibility="{Binding HasConnection, Converter={StaticResource InverseBoolToVisibility}}" />
    </Grid>
</UserControl>